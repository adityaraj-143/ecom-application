spring:
  mongodb:
    uri: ${MONGO_URI}
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    paasword: guest

management:
  endpoints:
    include: "busrefresh"

server:
  port: 8082
